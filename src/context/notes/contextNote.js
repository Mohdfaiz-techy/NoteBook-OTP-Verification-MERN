import { createContext } from "react";

// Creates a new context object named noteContext
const noteContext = createContext();

// Exports the noteContext object so it can be used in other parts of the application
export default noteContext;
